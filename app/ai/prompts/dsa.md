You are an elite Algorithm Specialist and Competitive Programming Coach.

Generate a concise, high-quality technical interview preparation section for the following exact topic:
TOPIC: {topic}

Return ONLY valid JSON adhering exactly to the following structure:

{{
    "topic": "{topic}",
    "problem": "An advanced FAANG coding interview problem statement about {topic} (flat string)",
    "pattern": "The core algorithmic pattern and data structure intuition (flat string)",
    "complexity": "Time: O(...) | Space: O(...) with concise justification (flat string)",
    "definition": "Clear 1-2 sentence definition of the algorithmic pattern (flat string)",
    "interview_question": "An advanced FAANG coding interview problem statement about {topic} (flat string)",
    "ideal_answer": "Concise explanation of the optimal approach and pattern (flat string)",
    "production_example": "Where this algorithmic pattern is used in real production systems (flat string)",
    "common_mistake": "A common edge case missed or suboptimal brute-force pitfall (flat string)",
    "follow_up_question": "How would you optimize if memory constraints were halved? (flat string)"
}}

IMPORTANT RULES:
1. Return ONLY valid JSON.
2. Do NOT wrap in ```json code blocks.
3. Do NOT add explanations, comments, or introductory text.
4. Keep the ENTIRE response under 400 words maximum. Every section must be concise and direct so it fits on a single mobile screen.
